# Colab·백테스트 코딩 스타일 가이드

이 문서는 이 저장소의 Python 및 Google Colab 백테스트를 일관되고 읽기 쉽게
작성하기 위한 기준이다. 시각화 세부 규칙은
[`VISUALIZATION_STYLE_GUIDE.md`](./VISUALIZATION_STYLE_GUIDE.md)를 함께 따른다.

## 1. 기본 원칙

- 사용자가 위에서 아래로 한 번 실행하면 결과를 얻을 수 있게 구성한다.
- 계산 정확성, 읽기 쉬운 흐름, 재현성을 장식이나 과도한 추상화보다 우선한다.
- 입력, 데이터 수집, 계산, 시각화, 저장, 화면 출력의 책임을 구분한다.
- 동일한 정보와 조건을 여러 곳에 반복하지 않는다.
- 현재 사용하지 않는 함수, 인자, 상수, import와 호환성 코드는 남겨두지 않는다.
- 입력값이 바뀌어도 제목, 축, 표, 파일명이 자동으로 정합성을 유지해야 한다.

## 2. Colab 노트북 구성

노트북은 다음 순서를 기본으로 한다.

1. 짧은 Markdown 제목과 실행 안내
2. 한글 입력 폼
3. 목적과 데이터 출처를 설명하는 짧은 docstring
4. import와 실행 환경 설정
5. 데이터 수집 함수
6. 계산 함수
7. 그래프와 표 함수
8. 파일 저장 및 다운로드 함수
9. 입력 검증과 `main()` 실행

관련 코드가 짧고 항상 함께 실행된다면 한 개의 Colab 폼 셀로 제공할 수 있다.
여러 셀로 나눌 때도 숨은 실행 순서 의존성을 만들지 않는다.

### 2.1 시작 안내

시작 안내는 사용자가 무엇을 입력하고 어떤 결과를 받는지만 설명한다.
내부 출력 경로 같은 구현 세부사항은 넣지 않는다.

```markdown
투자 조건을 입력하고 아래 코드 셀을 실행하세요. 시작연도별 생존 여부와
종료 잔액을 계산하고, 요약 CSV·월별 상세 CSV·PNG 그래프를 생성합니다.
```

### 2.2 입력 폼

- 사용자가 보는 변수는 한글 이름과 익숙한 단위를 사용한다.
- 금액은 `억원`, `만원`, 금리는 `퍼센트`, 기간은 `년` 단위로 받는다.
- 계산용 원화·비율 값은 입력 직후 한 번만 변환한다.
- 허용 범위를 아는 값에는 `min`, `step`을 지정한다.

```python
# @title 백테스트 조건을 입력하고 실행하세요
투자종목 = "QQQ"  # @param {type:"string"}
투자기간_년 = 10  # @param {type:"integer", min:1, step:1}
초기준비금_억원 = 1.0  # @param {type:"number", min:0.01, step:0.1}
월인출액_만원 = 100  # @param {type:"number", min:0, step:10}

INITIAL_RESERVE_KRW = float(초기준비금_억원) * WON_PER_EOK
MONTHLY_WITHDRAWAL_KRW = float(월인출액_만원) * WON_PER_MANWON
```

## 3. 설명과 주석

- 노트북 docstring에는 입력 폼과 중복되는 기본값을 나열하지 않는다.
- 데이터 출처, 가격 조정 방식, 핵심 계산 방식, 제외 항목은 반드시 남긴다.
- 코드가 그대로 말해주는 내용을 주석으로 반복하지 않는다.
- 외부 API의 종료일 미포함처럼 실수하기 쉬운 이유만 주석으로 설명한다.
- `QQQ` 전용 함수가 아니라면 설명에도 `선택 종목` 또는 `수정주가`라고 쓴다.

권장 형식:

```python
"""미국 ETF의 시작연도별 정기 인출 결과를 비교한다.

선택 종목 수익률은 Yahoo Finance 수정주가(배당·주식분할 반영),
원/달러 환율은 미국 연준 FRED DEXKOUS를 사용한다. 세금,
환전·거래 수수료와 물가상승률은 반영하지 않는다.

초기 준비금을 시작 시점 환율로 달러 환산해 선택 종목에 투자하고,
매월 말 당시 환율을 적용해 입력한 원화 금액만큼 인출한다.
"""
```

## 4. 이름과 단위

- 사용자 입력은 한글, 내부 계산 변수와 함수는 의미가 분명한 영문을 사용한다.
- 금액 변수에는 `_krw`, 달러 금액에는 `_usd`, 비율에는 `_rate`를 붙인다.
- 기간 변수에는 `_years`, `_months`, 연도에는 `_year`를 사용한다.
- 단위를 숫자에 반복해서 직접 쓰지 말고 이름 있는 상수를 사용한다.

```python
WON_PER_EOK = 100_000_000
WON_PER_MANWON = 10_000
MONTHS_PER_YEAR = 12
```

- 사용자가 선택할 수 있는 종목을 함수 설명이나 오류 메시지에서 특정 종목으로
  단정하지 않는다.
- 파일명은 소문자 종목과 기간을 포함해 입력값과 일치시킨다.

```python
prefix = f"{TICKER.lower()}_{INVESTMENT_YEARS}y"
```

## 5. 함수와 데이터 흐름

- 함수 하나는 데이터 수집, 계산, 표현, 저장 중 한 가지 책임을 맡는다.
- 함수명은 동작과 결과가 드러나게 짓는다.
- 한 곳에서만 쓰더라도 복잡한 계산이나 표현 로직은 이름 있는 함수로 분리한다.
- 반대로 한 줄을 감싸기만 하거나 의미를 추가하지 않는 함수는 만들지 않는다.
- 항상 같은 값만 전달하는 인자는 제거하고 함수 내부의 명확한 상수로 둔다.
- 전역 입력값은 진입점에서 함수 인자로 넘기고, 핵심 계산 함수는 가능한 한
  입력과 반환값만으로 이해할 수 있게 한다.
- 원본 DataFrame은 숫자형을 유지하고, 화면 표시용 복사본에서만 문자열로
  변환한다.
- 요약 결과와 월별 상세 결과를 명확히 구분한다.

권장 실행 흐름:

```text
입력 검증 → 시장 데이터 수집 → 백테스트 계산 → 파일 저장 → 화면 표시
```

## 6. 중복과 불필요한 코드 점검

수정 후 다음 항목을 반드시 확인한다.

- 호출되지 않는 함수가 있는가?
- 읽히지 않는 변수나 반환값을 불필요하게 구조 분해하고 있는가?
- 실제로 한 값만 받는 선택 인자가 있는가?
- 같은 조건을 한 반복문 안에서 여러 번 계산하는가?
- 같은 숫자 단위를 여러 위치에 직접 작성했는가?
- 같은 제목, 기간, 금액이 문자열로 하드코딩되어 있는가?
- 로컬 CLI용 코드가 Colab 전용 노트북에 남아 있는가?
- 이전 출력 형식을 위한 죽은 코드가 남아 있는가?

리팩터링은 동작을 바꾸지 않는 작은 단위로 수행한다. 짧게 만드는 것보다
의도가 바로 읽히는 것이 중요하며, 서로 다른 의미의 로직을 억지로 합치지 않는다.

## 7. 입력 검증과 오류

- 네트워크 요청 전에 입력값을 검증한다.
- 오류 메시지는 어떤 값이 왜 잘못됐는지 한글로 설명한다.
- 시작연도 범위, 기간, 초기 금액, 인출액의 경계를 확인한다.
- 필요한 월말 데이터 개수와 결측 여부를 계산 전에 확인한다.
- 데이터 다운로드 결과가 비어 있으면 종목명을 포함한 명확한 오류를 낸다.
- 잔액은 부동소수점 오차로 음수가 되지 않게 `max(..., 0.0)`으로 제한한다.
- 실제 인출액은 잔액을 초과하지 않게 제한한다.

## 8. 출력 파일

- Colab 결과는 `/content/output`, 로컬 실행은 `output`에 저장한다.
- 디렉터리는 저장 직전에 `mkdir(parents=True, exist_ok=True)`로 만든다.
- 요청된 결과물만 생성한다. 사용하지 않는 중간 파일은 만들지 않는다.
- CSV는 Excel에서도 한글이 깨지지 않도록 `utf-8-sig`를 사용한다.
- 요약 CSV에는 시작연도별 핵심 결과를, 상세 CSV에는 월별 계산 근거를 담는다.
- 파일 저장 함수는 생성된 `Path`를 반환하고 화면 출력에서 재사용한다.

```python
return summary_path, detail_path, chart_path
```

## 9. Colab 화면 출력과 다운로드

- 실행 조건, 요약표, 그래프, 다운로드 순서로 표시한다.
- 화면 표에는 독자가 판단하는 데 필요한 열만 넣고 상세 데이터는 CSV로 제공한다.
- 유지와 고갈은 절제된 기호로 구분한다.
- 값이 없는 고갈 시점은 `-`로 표시한다.
- 화면 표의 열 순서는 목적에 맞게 명시적으로 구성한다.

```text
시작연도 → 결과 → 고갈 시점 → 총 인출액 → 최종 잔액
```

- Colab에서 `FileLink`만 사용하면 `localhost` 경로가 열릴 수 있다.
- Colab에서는 `google.colab.files.download()`를 버튼 콜백에서 호출한다.
- 로컬 Jupyter에서는 `FileLink`를 대체 수단으로 사용할 수 있다.

```python
def display_download_button(label: str, path: Path) -> None:
    from IPython.display import FileLink, display

    if IS_COLAB:
        import ipywidgets as widgets
        from google.colab import files

        button = widgets.Button(description=f"{label} 다운로드", icon="download")
        button.on_click(lambda _: files.download(str(path)))
        display(button)
    else:
        display(FileLink(str(path)))
```

## 10. 표와 그래프

- 표와 그래프의 색상, 글꼴, 여백, 숫자 형식은
  `VISUALIZATION_STYLE_GUIDE.md`를 따른다.
- 제목과 소제목은 가까이 배치하고, 범례와는 겹치지 않게 한다.
- 제목, 축, 기준선 문구에는 입력한 기간과 금액을 동적으로 반영한다.
- 축에는 `준비금`보다 의미가 자연스러운 경우 `잔액`을 사용한다.
- 기준선은 `시작 준비금 1억원`처럼 금액까지 직접 표시한다.
- 동일한 정보를 범례와 그래프 직접 라벨에 중복해 넣지 않는다.
- 각주는 데이터 반영 여부와 제외 조건을 짧게 설명한다.

## 11. 코드 형식

- PEP 8을 기본으로 하며 함수와 변수에 일관된 `snake_case`를 사용한다.
- 공개 함수에는 한 줄짜리 명확한 docstring을 작성한다.
- 긴 함수 호출과 문자열은 괄호를 사용해 의미 단위로 줄바꿈한다.
- 여러 조건을 한 줄에 압축해 읽기 어려워지면 여러 줄로 펼친다.
- 타입 힌트는 함수의 입력과 반환 구조를 이해하는 데 도움이 될 때 사용한다.
- 동적 딕셔너리 행처럼 타입을 엄밀히 쓰는 비용이 큰 곳은 과도하게 복잡한
  타입 선언을 피한다.

## 12. 완료 전 검증

최소 검증 항목은 다음과 같다.

1. 노트북 JSON 파싱
2. 모든 코드 셀 Python 문법 컴파일
3. 대표 입력으로 계산 스모크 테스트
4. 요약표의 열, 문구, 숫자 형식 확인
5. 그래프 렌더링 및 잘림·겹침 확인
6. 요청한 파일만 생성되는지 확인
7. Colab 다운로드 버튼과 로컬 대체 링크 확인
8. 사용되지 않는 함수, import, 변수, 인자 재점검

네트워크 접근이 어려운 환경에서는 저장된 결과나 합성 시계열로 계산, 표,
그래프를 우선 검증하고 실제 데이터 다운로드 검증 여부를 별도로 기록한다.
